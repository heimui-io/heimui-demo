<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. This file lives in src/debug, so a release build never sees it and keeps
  Android's default: cleartext HTTP is refused outright.

  It exists because iterating on payloads locally means serving them over plain HTTP:

      python3 -m http.server 8080          # from the repo root
      StaticDemoCatalogRepository.sduiBaseUrl = "http://10.0.2.2:8080/sdui"

  10.0.2.2 is the emulator's alias for the host machine's loopback. Without this exemption the
  SDK reports "CLEARTEXT communication not permitted", which reads like an SDK fault and is not.

  Scoped to loopback addresses on purpose: a blanket cleartextTrafficPermitted="true" would also
  allow a debug build to be silently downgraded on any network it joins.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
